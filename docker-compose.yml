version: '3'

services:
  app:
    build: .
    ports:
      - "80:3000"
    depends_on:
      - mongo
    restart: always

  mongo:
    image: mongo:6
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
